<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dwt.redis.mapper.SeckillMapper">

    <insert id="createOrder" parameterType="com.dwt.redis.entity.SeckillOrder"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO seckill_order
            (user_id, product_id, quantity, price, status, create_time)
        VALUES
            (#{userId}, #{productId}, #{quantity}, #{price}, #{status}, #{createTime})
    </insert>

    <select id="getOrderById" parameterType="long"
            resultType="com.dwt.redis.entity.SeckillOrder">
        SELECT * FROM seckill_order WHERE id = #{id}
    </select>

    <update id="updateOrderStatus">
        UPDATE seckill_order
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <select id="checkUserPurchased" resultType="int">
        SELECT COUNT(*) FROM seckill_order
        WHERE user_id = #{userId} AND product_id = #{productId}
    </select>
</mapper>